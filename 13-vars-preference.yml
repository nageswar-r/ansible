- name: Arguments from script file
  hosts: local
  connection: local
  vars_files:
    - course.yml
  vars:
    name: COURSE
  tasks:
    - name: Print varibles name
      ansible.builtin.debug:
        msg: "{{ COURSE }}"